[[def: signature-verify, signature-verify gadget]]

~ A gadget verifying an issuer or counterparty signature over hidden credential content, in-circuit, so possession of a validly signed credential is proven without disclosing it.
