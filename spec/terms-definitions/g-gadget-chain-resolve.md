[[def: chain-resolve, chain-resolve gadget]]

~ A gadget proving that a chain of hidden grants nests — each hop’s scope within its parent’s, validity monotone, depth bounded — and terminates at a stated root.
