[[def: set root]]

~ A signed, published commitment to a set at a stated registry state — a membership root, a revocation root, an accredited-issuer root — against which membership or non-membership is proven in the presentation itself, so the verifier performs no live lookup.
