[[def: nullifier, nullifier gadget]]

~ A gadget deriving a deterministic, context-scoped pseudonym from a holder secret and a context descriptor, so reuse within one declared context is detectable and nothing links across contexts.
